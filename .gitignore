# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Credenciales
.env
.env.*
!.env.example

# Supabase CLI (guardan token/credenciales de conexión)
supabase/.temp
~/.supabase

# Backups y dumps con datos sensibles (solo copia local, NUNCA en el repo)
backups/

# Copias de trabajo de código (mantener fuera del repo)
src/*.backup*
src/*.antes*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Descargas de imágenes de trabajo (no versionar)
mockups_originales/
